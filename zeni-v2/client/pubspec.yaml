name: zeni_app
description: Zeni client
version: 2.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  speech_to_text: ^7.0.0
  flutter_tts: ^4.0.2
  shared_preferences: ^2.2.3
  geolocator: ^13.0.1
  url_launcher: ^6.3.1
  just_audio: ^0.9.40
  flutter_contacts: ^1.1.9+2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
